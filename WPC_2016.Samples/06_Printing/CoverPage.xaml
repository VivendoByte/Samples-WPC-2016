<Page
    x:Class="WPC_2016.Samples.Sample06.CoverPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrintSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="32">
            <TextBlock Text="ELENCO DELLE SESSIONI" Foreground="Red" FontSize="64" />
        </StackPanel>

        <Border BorderBrush="DarkGray" BorderThickness="4" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5 0.5">
            <TextBlock FontSize="80" Text="for eyes only" Foreground="Black" Margin="32 4 32 4" />
            <Border.RenderTransform>
                <RotateTransform Angle="-15" />
            </Border.RenderTransform>
        </Border>

        <TextBlock x:Name="Timestamp" Margin="0 0 16 16" FontSize="16" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
    </Grid>
</Page>